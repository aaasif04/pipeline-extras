## Variables that will be used to deploy the build in target hosts"
#Target Path for API venv.
temp_dir: /tmp/sds-insights-calib
file_name: sds-insight-apis-1.0-SNAPSHOT.zip

deploy_dir: /opt/sds-insights-calib/
#Port Number, that should use the API processs.
PORT: 10443
KILL: "/sbin/fuser -k"

log_dir: /var/log/sds-insights-calib/